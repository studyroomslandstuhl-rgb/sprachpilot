import {getActiveProfile,getActiveRole,loginUrlForCurrent,dashboardHref,logout} from "/js/auth.js?v=login-main-4";

const VERBS=(window.SP_FI_VERBS||[]).map((v,i)=>({...v,index:i+1}));
const GROUP_SIZE=Number(window.SP_FI_VERB_GROUP_SIZE)||20;
const GROUPS=[];for(let i=0;i<VERBS.length;i+=GROUP_SIZE)GROUPS.push({id:GROUPS.length+1,verbs:VERBS.slice(i,i+GROUP_SIZE)});
const TASKS=[['cards','Aa','Karteikarten'],['meaning-to-verb','B→V','Bedeutung → Verb'],['verb-to-meaning','V→B','Verb → Bedeutung'],['listen','🔊','Hören → Verb'],['image-to-verb','▣','Bild → Verb'],['verb-to-image','V→▣','Verb → Bild'],['read-sentence','📖','Lesen → Verb'],['verb-type','↔','Verbtyp'],['choose-form','du','Form auswählen'],['write-form','✎','Form schreiben'],['speak-form','🎙','Form sprechen'],['sentence','…','Satz ergänzen'],['exam','★','Gruppenprüfung']];
const LEARN=TASKS.slice(0,-1).map(x=>x[0]),TITLE=Object.fromEntries(TASKS.map(x=>[x[0],x[2]]));
const PERSONS=[['minä',0],['sinä',1],['hän',2],['me',3],['te',4],['he',5]].map(([label,i])=>({label,i}));
const app=document.querySelector('#app'),topbar=document.querySelector('#topbar');
const role=String(getActiveRole()||'').toLowerCase(),profile=getActiveProfile();if(!profile&&role!=='teacher')location.href=loginUrlForCurrent();
window.logout=logout;
let rec=null,currentQuestion=null,cardSolved=false;
const preview=()=>role==='teacher';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=v=>String(v??'').trim().toLocaleLowerCase('fi-FI').normalize('NFC').replace(/[.,!?;:“”"'`´()…]/g,'').replace(/\s+/g,' ');
const slug=v=>String(v||'').toLowerCase().replace(/ä/g,'ae').replace(/ö/g,'oe').replace(/ü/g,'ue').replace(/ß/g,'ss').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/^sich\s+/,'sich_').replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,'');
const shuffle=a=>{a=[...(a||[])];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a};
const options=(correct,pool,n=4)=>shuffle([correct,...shuffle(pool.filter(x=>norm(x)!==norm(correct))).slice(0,n-1)]);
function speak(text,slow=false){if(!('speechSynthesis'in window))return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='fi-FI';u.rate=slow?.55:.92;speechSynthesis.speak(u)}
function stopMic(){if(rec)try{rec.abort()}catch{}rec=null}

function imageCandidates(v){
 const key=v.img||slug(v.de),out=[`https://sprachpilot.b-cdn.net/${encodeURIComponent(key)}.webp`];
 try{if(typeof window.SP_VERB_IMAGE_OVERRIDE==='function'){const u=window.SP_VERB_IMAGE_OVERRIDE(v.de);if(u&&!out.includes(u))out.push(u)}}catch{}
 out.push(`https://sprachpilot.b-cdn.net/Neu/${encodeURIComponent(key)}.webp`);return [...new Set(out)];
}
window.SP_FI_IMAGE_ERROR=function(img){
 const v=VERBS[Number(img.dataset.fiVerbIndex)];if(!v)return;const list=imageCandidates(v);let i=Number(img.dataset.fiTry||0)+1;img.dataset.fiTry=String(i);if(i<list.length){img.src=list[i];return}img.hidden=true;img.nextElementSibling&&(img.nextElementSibling.hidden=false);
};
function image(v,compact=false){const src=imageCandidates(v)[0];return`<div class="verb-image ${compact?'compact':''}"><img data-fi-verb-index="${v.index-1}" data-fi-try="0" src="${esc(src)}" alt="Bild zu ${esc(v.fi)}" onerror="window.SP_FI_IMAGE_ERROR(this)"><div class="image-fallback" hidden><strong>${esc(v.fi)}</strong><span>${esc(v.de)}</span></div></div>`}

const FORMS={
 rakastaa:['rakastan','rakastat','rakastaa','rakastamme','rakastatte','rakastavat'],ostaa:['ostan','ostat','ostaa','ostamme','ostatte','ostavat'],ymmärtää:['ymmärrän','ymmärrät','ymmärtää','ymmärrämme','ymmärrätte','ymmärtävät'],tarvita:['tarvitsen','tarvitset','tarvitsee','tarvitsemme','tarvitsette','tarvitsevat'],kuulla:['kuulen','kuulet','kuulee','kuulemme','kuulette','kuulevat'],oppia:['opin','opit','oppii','opimme','opitte','oppivat'],asua:['asun','asut','asuu','asumme','asutte','asuvat'],tuoda:['tuon','tuot','tuo','tuomme','tuotte','tuovat'],olla:['olen','olet','on','olemme','olette','ovat'],kirjoittaa:['kirjoitan','kirjoitat','kirjoittaa','kirjoitamme','kirjoitatte','kirjoittavat'],valokuvata:['valokuvaan','valokuvaat','valokuvaa','valokuvaamme','valokuvaatte','valokuvaavat'],soittaa:['soitan','soitat','soittaa','soitamme','soitatte','soittavat'],kokata:['kokkaan','kokkaat','kokkaa','kokkaamme','kokkaatte','kokkaavat'],elää:['elän','elät','elää','elämme','elätte','elävät'],tulla:['tulen','tulet','tulee','tulemme','tulette','tulevat'],tavata:['tapaan','tapaat','tapaa','tapaamme','tapaatte','tapaavat'],mennä:['menen','menet','menee','menemme','menette','menevät'],uida:['uin','uit','ui','uimme','uitte','uivat'],etsiä:['etsin','etsit','etsii','etsimme','etsitte','etsivät'],tilata:['tilaan','tilaat','tilaa','tilaamme','tilaatte','tilaavat']
};
function verbType(w){w=String(w||'').toLocaleLowerCase('fi-FI');if(/(da|dä)$/.test(w))return 2;if(/(lla|llä|nna|nnä|rra|rrä|sta|stä)$/.test(w))return 3;if(/(ita|itä)$/.test(w))return 5;if(/(eta|etä)$/.test(w))return 6;if(/(ata|ätä|ota|ötä|uta|ytä)$/.test(w))return 4;return 1}
function harmony(w){return /[äöy]/.test(w)&&!/[aou]/.test(w)?'ä':'a'}
function genericForms(w){if(w==='tehdä')return['teen','teet','tekee','teemme','teette','tekevät'];if(w==='nähdä')return['näen','näet','näkee','näemme','näette','näkevät'];const t=verbType(w),a=harmony(w),vat=a==='ä'?'vät':'vat';if(t===2){const s=w.slice(0,-2);return[s+'n',s+'t',s,s+'mme',s+'tte',s+vat]}if(t===3){const s=w.slice(0,-1).replace(/ll$/,'l').replace(/nn$/,'n').replace(/rr$/,'r').replace(/st$/,'s')+'e';return[s+'n',s+'t',s+'e',s+'mme',s+'tte',s+vat]}if(t===5){const s=w.slice(0,-2)+'tse';return[s+'n',s+'t',s+'e',s+'mme',s+'tte',s+vat]}if(t===6){const s=w.slice(0,-2)+'ne';return[s+'n',s+'t',s+'e',s+'mme',s+'tte',s+vat]}return null}
const formsFor=v=>FORMS[v.fi]||genericForms(v.fi);const grammarTargets=g=>{const x=g.verbs.filter(v=>Array.isArray(formsFor(v)));return x.length?x:g.verbs};const personFor=v=>PERSONS[(v.index-1)%6];

const userSlug=()=>[profile?.email,profile?.courseCode,profile?.kurs,profile?.kursnummer,profile?.vorname,profile?.nachname].filter(Boolean).join('_').toLowerCase().replace(/[^a-z0-9äöüß]+/gi,'_')||'student';
const storageKey=()=>`SP_FI_VERB_GROUPS_PROGRESS_${userSlug()}`;
const blankTask=()=>({done:[],queue:[],current:null,tries:0,hadWrong:false});
const blankRun=()=>({tasks:{},awards:{tasks:{},examPoints:0,examPercent:0},exam:{bestPercent:0,stars:0,session:null}});
const blankGroup=()=>({currentRun:1,runs:{'1':blankRun()}});
let state=load();function load(){try{return JSON.parse(localStorage.getItem(storageKey())||'{}')||{}}catch{return{}}}function save(){if(preview())return;try{localStorage.setItem(storageKey(),JSON.stringify(state))}catch{}}
function gs(id){const k=String(id);if(!state[k])state[k]=blankGroup();if(!state[k].runs)state[k].runs={'1':blankRun()};return state[k]}
function run(id){const g=gs(id),k=String(g.currentRun||1);if(!g.runs[k])g.runs[k]=blankRun();return g.runs[k]}
function st(id,task){const r=run(id);if(!r.tasks[task])r.tasks[task]=blankTask();const s=r.tasks[task];s.done=Array.isArray(s.done)?s.done:[];s.queue=Array.isArray(s.queue)?s.queue:[];s.current=s.current||null;s.tries=Number(s.tries)||0;s.hadWrong=!!s.hadWrong;return s}
function targets(id,task){const g=GROUPS[id-1];if(!g)return[];return['choose-form','write-form','speak-form','sentence'].includes(task)?grammarTargets(g):g.verbs}
function byDe(id,task,de){return targets(id,task).find(v=>v.de===de)||null}
function percent(id,task){const t=targets(id,task),d=new Set(st(id,task).done);return t.length?Math.round(t.filter(v=>d.has(v.de)).length*100/t.length):100}
const done=(id,task)=>percent(id,task)>=100;const learnDone=id=>LEARN.every(t=>done(id,t));
function next(id,task){const s=st(id,task),t=targets(id,task);if(s.current&&!s.done.includes(s.current))return byDe(id,task,s.current);if(!s.queue.length)s.queue=shuffle(t.filter(v=>!s.done.includes(v.de)).map(v=>v.de));s.current=s.queue.shift()||null;s.tries=0;s.hadWrong=false;save();return byDe(id,task,s.current)}
function wrong(id,task){const s=st(id,task);s.tries++;s.hadWrong=true;save();return s.tries}
function right(id,task){const s=st(id,task),de=s.current;if(!de)return;if(s.hadWrong||s.tries>0){if(!s.done.includes(de)&&!s.queue.includes(de))s.queue.push(de)}else if(!s.done.includes(de))s.done.push(de);s.current=null;s.tries=0;s.hadWrong=false;if(done(id,task)&&!run(id).awards.tasks[task])run(id).awards.tasks[task]=5*(gs(id).currentRun||1);save()}
function points(id){return Object.values(gs(id).runs||{}).reduce((a,r)=>a+Object.values(r.awards?.tasks||{}).reduce((x,n)=>x+(Number(n)||0),0)+(Number(r.awards?.examPoints)||0),0)}
const totalPoints=()=>GROUPS.reduce((n,g)=>n+points(g.id),0);const stars=p=>p>=100?3:p>=70?2:p>=50?1:0;

function route(){const q=new URLSearchParams(location.search),group=Math.max(0,Math.min(GROUPS.length,Number(q.get('group'))||0)),raw=q.get('task')||'',task=TASKS.some(x=>x[0]===raw)?raw:'',view=q.get('view')==='overview'?'overview':'';return{group,task,view}}
function href(group=0,task='',view=''){const q=new URLSearchParams();if(group)q.set('group',group);if(task)q.set('task',task);if(view)q.set('view',view);return'/finnisch/verben/'+(q.toString()?'?'+q.toString():'')}
function go(x={}){history.pushState(null,'',href(x.group||0,x.task||'',x.view||''));render()}
function header(r){const p=profile||{},name=[p.vorname||p.firstName,p.nachname||p.lastName].filter(Boolean).join(' ')||(preview()?'Lehrer-Vorschau':'Schüler');const back=r.task&&r.group?href(r.group):(r.group||r.view?href():'/finnisch/');topbar.innerHTML=`<div class="topbar-main"><a class="brand" href="/finnisch/"><img src="/assets/logo/sprachpilot-logo.png" alt="SprachPilot"><div><h1>Verben</h1><p>${VERBS.length} Verben · ${GROUPS.length} Gruppen</p></div></a><div class="account-actions"><span class="account-pill">${esc(name)}</span><a class="btn secondary" href="${esc(dashboardHref())}">Dashboard</a><button class="btn secondary" data-action="logout">Abmelden</button></div></div><nav class="topnav"><a class="btn secondary" href="${back}">Zurück</a>${!r.task?`<button class="btn secondary ${r.view==='overview'?'active-nav':''}" data-action="overview">Übersicht</button>`:''}${r.group&&!r.task?`<button class="btn danger-btn" data-action="reset-group" data-group="${r.group}">Fortschritt löschen</button>`:''}</nav>`}
const previewNote=()=>preview()?'<div class="preview-note">Lehrer-Vorschau · nichts wird gespeichert</div>':'';
function score(id=0){if(preview())return'';if(!id)return`<section class="card score-card compact-score"><h2>${totalPoints()} Punkte</h2><span>gesamt</span></section>`;const r=run(id),sum=Object.values(r.awards.tasks||{}).reduce((a,n)=>a+(Number(n)||0),0);return`<section class="card score-card"><div><p class="eyebrow">Gruppe ${id}</p><h2>Runde ${gs(id).currentRun||1} von 3</h2><p>${sum} Aufgabenpunkte · ${Number(r.awards.examPoints)||0} Prüfungspunkte</p></div><div class="score-total">${points(id)}<span>Punkte</span></div></section>`}
function taskCards(id){const r=run(id);return TASKS.map((t,i)=>{const exam=t[0]==='exam',open=!exam||learnDone(id),p=exam?Number(r.exam.bestPercent)||0:percent(id,t[0]);return`<button class="task-card ${p>=100?'done-card':''} ${!open?'locked-task':''}" data-action="task" data-group="${id}" data-task="${t[0]}" ${open?'':'disabled'}><span class="task-number">${i+1}</span><span class="task-icon">${open?t[1]:'🔒'}</span><span class="task-title">${t[2]}</span><div class="task-mini-progress"><span style="width:${p}%"></span></div><span class="task-status">${open?(p>=100?'Fertig':p?`${p}%`:'Starten'):'Gesperrt'}</span></button>`}).join('')}
function panels(sel=0){return GROUPS.map(g=>{const r=run(g.id),d=LEARN.filter(t=>done(g.id,t)).length,e=Number(r.exam.bestPercent)||0;return`<details class="group-panel" data-group-panel="${g.id}" ${sel===g.id?'open':''}><summary data-action="group" data-group="${g.id}"><span class="group-number">Gruppe ${g.id}</span><span>${g.verbs.length} Verben</span><span>Runde ${gs(g.id).currentRun||1}/3</span><span>${d}/${LEARN.length} · Prüfung ${e}%</span></summary><div class="group-body"><div class="task-grid">${taskCards(g.id)}</div></div></details>`}).join('')}
function home(sel=0){app.innerHTML=`${previewNote()}${score(sel)}<section class="card"><div class="section-head"><h2>Gruppen</h2><span class="overview-total">${VERBS.length} Verben</span></div><div class="groups-accordion">${panels(sel)}</div></section>`;if(sel)setTimeout(()=>document.querySelector(`[data-group-panel="${sel}"]`)?.scrollIntoView({behavior:'smooth',block:'start'}),80)}
function overview(){app.innerHTML=`${previewNote()}<section class="card"><div class="section-head"><h2>Übersicht</h2><span class="overview-total">${VERBS.length} Verben</span></div><div class="overview-grid">${VERBS.map(v=>`<article class="overview-verb-card">${image(v,true)}<div class="overview-verb-text"><span class="group-badge">Gruppe ${Math.floor((v.index-1)/GROUP_SIZE)+1}</span><h3>${esc(v.fi)}</h3><p>${esc(v.de)}</p><button class="audio-mini" data-action="audio" data-text="${esc(v.fi)}">🔊</button></div></article>`).join('')}</div></section>`}
function progress(id,task){const t=targets(id,task),d=new Set(st(id,task).done),n=t.filter(v=>d.has(v.de)).length,p=percent(id,task);return`<div class="task-progress-row"><span>${n} richtig · ${Math.max(0,t.length-n)} übrig</span><strong>${p}%</strong></div><div class="mini-progress"><div style="width:${p}%"></div></div>`}
function grammar(task){if(!['verb-type','choose-form','write-form','speak-form','sentence'].includes(task))return'';return`<div class="question-support"><strong>Grammatik:</strong> ${task==='verb-type'?'Finnische Verben werden nach ihrer Infinitiv-Endung in sechs Verbtypen eingeteilt. Achte auf die Endung.':'Im Finnischen wird das Verb nach der Person verändert. Übe die Präsensformen für minä, sinä, hän, me, te und he.'}</div>`}
function page(id,task,body){return`<section class="card task-page"><div class="task-page-head"><div><p class="eyebrow">Gruppe ${id}</p><h2>${esc(TITLE[task])}</h2></div></div>${grammar(task)}${progress(id,task)}<div class="question-card">${body}<div id="feedback"></div></div></section>`}
function setFb(ok,html){const e=document.querySelector('#feedback');if(e){e.className='feedback '+(ok?'ok':'no');e.innerHTML=html}}
function wrongFb(id,task,solution,hint){const n=wrong(id,task);setFb(false,n===1?'Noch nicht richtig.':n===2?`Hilfe: ${esc(hint||('Die Lösung beginnt mit „'+String(solution).charAt(0)+'“.'))}`:`Lösung: <strong>${esc(solution)}</strong>`)}
function correct(id,task){const had=st(id,task).hadWrong||st(id,task).tries>0;right(id,task);setFb(true,had?'Richtig. Diese Aufgabe wird später noch einmal wiederholt.':'Richtig!');setTimeout(()=>task(id,task),650)}
function mc(opts){return`<div class="option-grid">${opts.map(o=>`<button class="option" data-answer="${esc(o.value)}">${esc(o.label)}</button>`).join('')}</div>`}
function imgs(opts){return`<div class="image-choice-grid">${opts.map(v=>`<button class="image-option" data-answer="${esc(v.de)}">${image(v,true)}</button>`).join('')}</div>`}
function input(ph='Antwort schreiben'){return`<div class="answer-form"><div class="answer-row"><input id="answerInput" autocomplete="off" autocapitalize="none" placeholder="${esc(ph)}"><button class="btn" id="checkInput">Kontrollieren</button></div></div>`}

function cards(id){const taskName='cards',v=next(id,taskName);if(!v)return finish(id,taskName);cardSolved=false;currentQuestion={v,answer:v.fi};app.innerHTML=`<section class="card task-page"><div class="task-page-head"><div><p class="eyebrow">Gruppe ${id}</p><h2>Karteikarten</h2></div></div>${progress(id,taskName)}<div class="flip-wrap"><div id="verbFlipCard" class="flip-card" role="button" tabindex="0"><div class="flip-face flip-front">${image(v)}</div><div class="flip-face flip-back"><div class="flip-word">${esc(v.fi)}</div><div class="flip-note">Lösung</div><button type="button" class="btn secondary card-listen-btn" id="cardListenBtn">🔊 Anhören</button></div></div></div><div class="hint card-translation">Übersetzung: <b>${esc(v.de)}</b></div><div class="actions card-actions"><button id="cardMicBtn" type="button" class="btn">Sprechen</button><button id="cardWriteBtn" type="button" class="btn secondary">Schreiben</button></div><div id="cardMicStatus" class="small card-mic-status"></div><div id="cardAnswerBox" class="card-answer-box" hidden><div class="answer-row"><input id="cardAnswerInput" autocomplete="off" autocapitalize="none" placeholder="Verb schreiben"><button id="cardCheckBtn" type="button" class="btn">Kontrollieren</button></div></div><div id="cardFeedback"></div><div id="cardAfter" class="actions card-actions"></div></section>`;
 const flip=()=>{if(cardSolved||flip.dataset.used==='1')return;flip.dataset.used='1';document.querySelector('#verbFlipCard')?.classList.add('flipped');wrong(id,taskName);const f=document.querySelector('#cardFeedback');f.className='feedback no';f.textContent='Als Hilfe aufgedeckt. Diese Karte wird später wiederholt.';const a=document.querySelector('#cardAfter');a.innerHTML='<button type="button" class="btn" id="cardHelpNext">Weiter</button>';document.querySelector('#cardHelpNext').onclick=()=>{right(id,taskName);cards(id)}};
 const card=document.querySelector('#verbFlipCard');card.addEventListener('click',e=>{if(!e.target.closest('button,input,a'))flip()});card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();flip()}});
 document.querySelector('#cardListenBtn').onclick=e=>{e.stopPropagation();speak(v.fi)};document.querySelector('#cardMicBtn').onclick=()=>speech(x=>checkCard(id,x));document.querySelector('#cardWriteBtn').onclick=()=>{document.querySelector('#cardAnswerBox').hidden=false;document.querySelector('#cardAnswerInput').focus()};document.querySelector('#cardCheckBtn').onclick=()=>checkCard(id,document.querySelector('#cardAnswerInput').value);document.querySelector('#cardAnswerInput').onkeydown=e=>{if(e.key==='Enter')checkCard(id,e.target.value)};
}
function checkCard(id,value){if(cardSolved||!currentQuestion)return;const s=st(id,'cards'),v=currentQuestion.v;if(norm(value)===norm(v.fi)){cardSolved=true;const had=s.hadWrong||s.tries>0;document.querySelector('#verbFlipCard')?.classList.add('flipped');right(id,'cards');const f=document.querySelector('#cardFeedback');f.className='feedback ok';f.textContent=had?'Richtig. Diese Karte wird später noch einmal wiederholt.':'Richtig!';const a=document.querySelector('#cardAfter');a.innerHTML='<button type="button" class="btn" id="cardNextBtn">Weiter</button>';document.querySelector('#cardNextBtn').onclick=()=>cards(id)}else{const n=wrong(id,'cards');const f=document.querySelector('#cardFeedback');f.className='feedback no';f.innerHTML=n===1?'Noch nicht richtig.':n===2?`Hilfe: Das Wort beginnt mit „${esc(v.fi.charAt(0))}“.`:`Lösung: <strong>${esc(v.fi)}</strong>`}}
function speech(cb){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){const e=document.querySelector('#feedback')||document.querySelector('#cardMicStatus');if(e)e.textContent='Mikrofon wird nicht unterstützt. Bitte schreibe die Antwort.';return}try{stopMic();rec=new SR();rec.lang='fi-FI';rec.interimResults=false;rec.maxAlternatives=5;rec.onresult=e=>cb(e.results?.[0]?.[0]?.transcript||'');rec.onerror=()=>{const x=document.querySelector('#feedback')||document.querySelector('#cardMicStatus');if(x)x.textContent='Mikrofon hat nicht funktioniert. Bitte schreibe die Antwort.'};rec.onend=()=>rec=null;rec.start()}catch{}}

function build(id,taskName,v){const g=GROUPS[id-1],fi=g.verbs.map(x=>x.fi),de=g.verbs.map(x=>x.de);if(taskName==='meaning-to-verb')return{kind:'mc',prompt:`Was heißt „${v.de}“ auf Finnisch?`,answer:v.fi,opts:options(v.fi,fi).map(x=>({value:x,label:x})),hint:`Das Wort beginnt mit „${v.fi.charAt(0)}“.`};if(taskName==='verb-to-meaning')return{kind:'mc',prompt:`Was bedeutet „${v.fi}“?`,answer:v.de,opts:options(v.de,de).map(x=>({value:x,label:x})),hint:`Die Bedeutung beginnt mit „${v.de.charAt(0)}“.`};if(taskName==='listen')return{kind:'mc',prompt:'Höre und wähle das richtige finnische Verb.',answer:v.fi,audio:v.fi,opts:options(v.fi,fi).map(x=>({value:x,label:x})),hint:`Das Wort beginnt mit „${v.fi.charAt(0)}“.`};if(taskName==='image-to-verb')return{kind:'mc',prompt:'Welches finnische Verb passt zum Bild?',answer:v.fi,image:v,opts:options(v.fi,fi).map(x=>({value:x,label:x})),hint:`Das Wort beginnt mit „${v.fi.charAt(0)}“.`};if(taskName==='verb-to-image')return{kind:'images',prompt:`Welches Bild passt zu „${v.fi}“?`,answer:v.de,opts:shuffle([v,...shuffle(g.verbs.filter(x=>x.de!==v.de)).slice(0,3)]),hint:`Die deutsche Bedeutung ist „${v.de}“.`};if(taskName==='read-sentence')return{kind:'mc',prompt:`Lies: „Haluan ${v.fi}.“ Was bedeutet das Verb?`,answer:v.de,opts:options(v.de,de).map(x=>({value:x,label:x})),hint:`${v.fi} bedeutet „${v.de}“.`};if(taskName==='verb-type'){const t=verbType(v.fi);return{kind:'mc',prompt:`Zu welchem finnischen Verbtyp gehört „${v.fi}“?`,answer:String(t),opts:[1,2,3,4,5,6].map(n=>({value:String(n),label:`Verbtyp ${n}`})),hint:`Achte auf die Endung von „${v.fi}“.`}}if(['choose-form','write-form','speak-form','sentence'].includes(taskName)){const f=formsFor(v),p=personFor(v),ans=f?f[p.i]:v.fi;if(taskName==='choose-form')return{kind:'mc',prompt:`${p.label} — ${v.fi}. Wähle die richtige Präsensform.`,answer:ans,opts:options(ans,f||fi).map(x=>({value:x,label:x})),hint:`Gesucht ist die Form für „${p.label}“.`};if(taskName==='write-form')return{kind:'input',prompt:`Schreibe die Präsensform: ${p.label} — ${v.fi}`,answer:ans,hint:`Gesucht ist die Form für „${p.label}“.`};if(taskName==='speak-form')return{kind:'speech',prompt:`Sprich die Präsensform: ${p.label} — ${v.fi}`,answer:ans,hint:`Gesucht ist die Form für „${p.label}“.`};return{kind:'input',prompt:`Ergänze: ${p.label} ___ nyt. (${v.de}; Infinitiv: ${v.fi})`,answer:ans,hint:`Setze ${v.fi} in die Form für „${p.label}“.`}}return null}
function task(id,taskName){if(taskName==='cards')return cards(id);if(taskName==='exam')return exam(id);if(done(id,taskName))return finish(id,taskName);const v=next(id,taskName);if(!v)return finish(id,taskName);const q=build(id,taskName,v);currentQuestion={...q,v};let media=q.image?image(q.image):q.audio?`<div class="listen-box"><button class="btn" id="play">🔊 Hören</button><button class="btn secondary" id="slow">Langsam</button></div>`:'';let ans=q.kind==='mc'?mc(q.opts):q.kind==='images'?imgs(q.opts):q.kind==='input'?input():`<div class="speech-box"><div class="actions"><button class="btn" id="speakAnswer">🎤 Sprechen</button><button class="btn secondary" id="writeFallback">✍️ Schreiben</button></div><div id="writeBox" class="answer-form hidden">${input('Verbform schreiben')}</div></div>`;app.innerHTML=page(id,taskName,`${media}<div class="question">${esc(q.prompt)}</div>${ans}`);document.querySelector('#play')?.addEventListener('click',()=>speak(q.audio));document.querySelector('#slow')?.addEventListener('click',()=>speak(q.audio,true));document.querySelectorAll('[data-answer]').forEach(b=>b.addEventListener('click',()=>answer(id,taskName,b.dataset.answer)));const inp=document.querySelector('#answerInput');document.querySelector('#checkInput')?.addEventListener('click',()=>answer(id,taskName,inp?.value||''));inp?.addEventListener('keydown',e=>{if(e.key==='Enter')answer(id,taskName,e.target.value)});document.querySelector('#speakAnswer')?.addEventListener('click',()=>speech(x=>answer(id,taskName,x)));document.querySelector('#writeFallback')?.addEventListener('click',()=>{document.querySelector('#writeBox')?.classList.remove('hidden');setTimeout(()=>document.querySelector('#answerInput')?.focus(),20)})}
function answer(id,taskName,value){const q=currentQuestion;if(!q)return;if(norm(value)===norm(q.answer))correct(id,taskName);else wrongFb(id,taskName,q.answer,q.hint)}
function finish(id,taskName){if(done(id,taskName)&&!run(id).awards.tasks[taskName]){run(id).awards.tasks[taskName]=5*(gs(id).currentRun||1);save()}app.innerHTML=`<section class="card"><div class="finish-box"><div class="finish-icon">✓</div><h2>Gut gemacht!</h2><p>${Number(run(id).awards.tasks[taskName])||0} Punkte · Runde ${gs(id).currentRun||1}</p></div></section>`}
function exam(id){if(!learnDone(id)){app.innerHTML='<section class="card locked-card"><h2>🔒 Gruppenprüfung</h2><p>Die Prüfung wird freigeschaltet, wenn alle Lernaufgaben 100 % erreicht haben.</p></section>';return}const r=run(id);if(!r.exam.session){const qs=GROUPS[id-1].verbs.slice(0,20).map((v,i)=>build(id,['meaning-to-verb','verb-to-meaning','image-to-verb','verb-type'][i%4],v));r.exam.session={i:0,c:0,qs:qs.map(q=>({kind:q.kind,prompt:q.prompt,answer:q.answer,opts:q.opts,imageDe:q.image?.de||''}))};save()}const s=r.exam.session;if(s.i>=s.qs.length)return finishExam(id);const q=s.qs[s.i],v=q.imageDe?GROUPS[id-1].verbs.find(x=>x.de===q.imageDe):null;app.innerHTML=`<section class="card task-page"><div class="task-page-head"><div><p class="eyebrow">Gruppe ${id}</p><h2>★ Gruppenprüfung</h2></div></div><div class="task-progress-row"><span>Frage ${s.i+1} von ${s.qs.length}</span><strong>${s.c} richtig</strong></div><div class="question-card">${v?image(v):''}<div class="question">${esc(q.prompt)}</div>${q.kind==='mc'?mc(q.opts):''}</div></section>`;document.querySelectorAll('[data-answer]').forEach(b=>b.addEventListener('click',()=>{if(norm(b.dataset.answer)===norm(q.answer))s.c++;s.i++;save();exam(id)}))}
function finishExam(id){const r=run(id),s=r.exam.session,total=s.qs.length,p=Math.round(s.c*100/total),old=Number(r.exam.bestPercent)||0;if(p>old){r.exam.bestPercent=p;r.exam.stars=stars(p);r.awards.examPercent=p;r.awards.examPoints=p*(gs(id).currentRun||1)}r.exam.session=null;save();app.innerHTML=`<section class="card"><div class="finish-box"><div class="exam-percent">${p}%</div><div class="stars">${'★'.repeat(stars(p))}${'☆'.repeat(3-stars(p))}</div><h2>Prüfung beendet</h2><p>Bestes Ergebnis: ${r.exam.bestPercent}%</p></div></section>`}

function bind(){document.querySelectorAll('[data-action]').forEach(el=>{if(el.dataset.bound)return;el.dataset.bound='1';el.addEventListener('click',e=>{const b=e.currentTarget,a=b.dataset.action;if(a==='logout')return logout();if(a==='overview')return go({view:'overview'});if(a==='group')return go({group:Number(b.dataset.group)||0});if(a==='task')return go({group:Number(b.dataset.group)||0,task:b.dataset.task||''});if(a==='audio')return speak(b.dataset.text||'');if(a==='reset-group'){const id=Number(b.dataset.group)||0;if(id&&confirm('Fortschritt dieser Gruppe wirklich löschen?')){delete state[String(id)];save();render()}}})})}
function render(){stopMic();const r=route();header(r);if(r.view==='overview')overview();else if(r.group&&r.task)task(r.group,r.task);else home(r.group||0);bind()}
window.addEventListener('popstate',render);render();
