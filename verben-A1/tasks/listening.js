function speakTextVerb(t,slow=false){try{const u=new SpeechSynthesisUtterance(String(t||""));u.lang="de-DE";u.rate=slow?0.6:0.9;speechSynthesis.cancel();speechSynthesis.speak(u)}catch(e){alert("Hören wird auf diesem Gerät nicht unterstützt.")}}
function hearWrite(){const v=nextFromTaskQueue("hoeren_schreiben");if(!v){renderHome();return}$("app").innerHTML=`<h2>Hören → Schreiben</h2>${taskProgressHtml("hoeren_schreiben","Hören → Schreiben")}<button onclick="speakTextVerb('${safeText(v)}')">Hören</button><button class="secondary" onclick="speakTextVerb('${safeText(v)}',true)">Langsam hören</button><input id="hearInput" placeholder="Verb schreiben"><button class="success" onclick="checkHearWrite('${safeText(v)}')">Prüfen</button><div id="fb"></div>`}
function checkHearWrite(v){const good=clean($("hearInput").value)===clean(v);$("fb").innerHTML=good?"<div class='ok'>Richtig.</div>":`<div class='no'>Falsch. Richtig ist: <strong>${safeText(v)}</strong></div>`;addEncounter(v,"hoeren_schreiben",good);finishQueuedVerb("hoeren_schreiben",v,good);setTimeout(hearWrite,800)}
function hearSpeak(){const v=nextFromTaskQueue("hoeren_sprechen");if(!v){renderHome();return}$("app").innerHTML=`<h2>Hören → Sprechen</h2>${taskProgressHtml("hoeren_sprechen","Hören → Sprechen")}<button onclick="speakTextVerb('${safeText(v)}')">Hören</button><input id="speakFallback" placeholder="Falls Mikro nicht geht: Verb schreiben"><button class="success" onclick="checkHearSpeak('${safeText(v)}')">Prüfen</button><div id="fb"></div>`}
function checkHearSpeak(v){const good=clean($("speakFallback").value)===clean(v);$("fb").innerHTML=good?"<div class='ok'>Richtig.</div>":`<div class='no'>Richtig wäre: <strong>${safeText(v)}</strong></div>`;addEncounter(v,"hoeren_sprechen",good);finishQueuedVerb("hoeren_sprechen",v,good);setTimeout(hearSpeak,800)}
