// Verben A1 arbeitet beim Üben lokal, damit die Seite schnell bleibt und Fortschritte beim Neuladen nicht durch alte Cloud-Daten überschrieben werden.
// Die zentrale Speicherung läuft über js/cloud-progress-sync.js; auf Übungsseiten ist Cloud-Sync im Performance-Modus deaktiviert.
const db = null;
