// Disabled: global topbar-standard used to rewrite headers repeatedly.
// Local pages now keep their own header to prevent layout jumps.
window.SprachPilotTopbarStandard={disabled:true,reason:'local-page-headers-only'};