import { db, doc, serverTimestamp } from '/js/firebase.js';
import { getActiveProfile, getActiveRole } from '/js/auth.js';
import '/shared/points-recalculator.js?v=2';
import { runTransaction } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js';

const E=window.VerbGroupsEngine;
const CARRY_ID='verben-recovered-points';
let syncing=false,timer=0;
const point=v=>{const n=Number(v);return Number.isFinite(n)&&n>0?n:0};
const cleanId=s=>String(s||'').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'item';
const uniq=a=>[...new Set((a||[]).filter(Boolean).map(String))];
function profile(){return getActiveProfile()||window.VerbGroupsProfile||{}}
function course(p=profile()){return p.kurs||p.kursnummer||p.courseCode||p.course||localStorage.getItem('SP_COURSE_CODE')||''}
function fallbackId(p=profile()){return cleanId((p.courseDocId||course(p)||'kurs')+'_'+(String(p.email||'').trim().toLowerCase()||p.vorname||p.firstName||'student'))}
function ids(p=profile()){const api=window.SPProgress;if(api?.idCandidates){try{const list=api.idCandidates(p);if(Array.isArray(list)&&list.length)return uniq(list)}catch{}}return uniq([p.canonicalStudentId,p.docId,p.studentId,p.userId,p.uid,p.id,...(Array.isArray(p.aliasIds)?p.aliasIds:[]),localStorage.getItem('SP_STUDENT_ID'),fallbackId(p)])}
function isPreview(){const role=String(getActiveRole()||localStorage.getItem('SP_LOGIN_ROLE')||'').toLowerCase();return ['teacher','lehrer','admin'].includes(role)||E?.isPreview?.()===true}
function storedPoints(progress={}){return Math.max(point(progress?.ranking?.points),point(progress?.totals?.points),point(progress?.pointsTotal),point(progress?.lifetimePoints),point(progress?.punkteGesamt),point(progress?.points),point(localStorage.getItem('SP_POINTS_TOTAL')))}
function displayName(p){return[p.vorname||p.firstName||p.name,p.nachname||p.lastName].filter(Boolean).join(' ')||p.displayName||p.email||'Schüler/in'}
async function getProgress(){if(!window.SPProgress){try{await import('/js/progress.js?v=20260831-central2')}catch{}}try{return await window.SPProgress?.loadCurrentStudentProgress?.()||{}}catch{return{}}}
function mergeRun(local={},cloud={}){const out={...cloud,...local},tasks={};for(const key of new Set([...Object.keys(cloud.tasks||{}),...Object.keys(local.tasks||{})])){const a=local.tasks?.[key]||{},b=cloud.tasks?.[key]||{},done=uniq([...(b.done||[]),...(a.done||[])]),total=Math.max(Number(a.total)||0,Number(b.total)||0);tasks[key]={...b,...a,done,total,completed:!!(a.completed||b.completed||(total>0&&done.length>=total))}}out.tasks=tasks;out.awards={tasks:{},examPoints:Math.max(Number(local.awards?.examPoints)||0,Number(cloud.awards?.examPoints)||0)};for(const key of new Set([...Object.keys(cloud.awards?.tasks||{}),...Object.keys(local.awards?.tasks||{})]))out.awards.tasks[key]=Math.max(Number(local.awards?.tasks?.[key])||0,Number(cloud.awards?.tasks?.[key])||0);out.exam={...(cloud.exam||{}),...(local.exam||{}),bestPercent:Math.max(Number(local.exam?.bestPercent)||0,Number(cloud.exam?.bestPercent)||0),stars:Math.max(Number(local.exam?.stars)||0,Number(cloud.exam?.stars)||0)};out.completed=!!(local.completed||cloud.completed);return out}
function mergeGroups(local={},cloud={}){const out={};for(const id of new Set([...Object.keys(cloud||{}),...Object.keys(local||{})])){const a=local?.[id]||{},b=cloud?.[id]||{},runs={};for(const runId of new Set([...Object.keys(b.runs||{}),...Object.keys(a.runs||{})]))runs[runId]=mergeRun(a.runs?.[runId]||{},b.runs?.[runId]||{});out[id]={...b,...a,signature:a.signature||b.signature||'',verbs:uniq([...(b.verbs||[]),...(a.verbs||[])]),currentRun:Math.max(Number(a.currentRun)||1,Number(b.currentRun)||1),runs}}return out}
function groupMetadata(){const out={};for(const group of E?.GROUPS||[]){const gs=E.groupState?.(group.id),runs={};for(const[runId,run]of Object.entries(gs?.runs||{})){const tasks={};for(const task of E.LEARN||[])tasks[task]={done:(run?.tasks?.[task]?.done||[]).slice(),total:Number(run?.tasks?.[task]?.total)||group.verbs.length,completed:(run?.tasks?.[task]?.done||[]).length>=group.verbs.length};runs[runId]={tasks,awards:{tasks:{...(run?.awards?.tasks||{})},examPoints:Number(run?.awards?.examPoints)||0},exam:{bestPercent:Number(run?.exam?.bestPercent)||0,stars:Number(run?.exam?.stars)||0},completed:!!run?.completed}}out[String(group.id).padStart(2,'0')]={signature:group.signature,verbs:group.verbs.slice(),currentRun:Number(gs?.currentRun)||1,runs}}return out}
function groupPoints(groups={}){let total=0;for(const group of Object.values(groups||{})){try{total+=point(window.SPPointRecalculator?.groupPoints?.(group)?.points)}catch{}}return total}
function evidence(progress={}){try{return point(window.SPPointRecalculator?.calculate?.(progress)?.total)}catch{return 0}}
async function sync(){
 if(syncing||isPreview()||!E?.GROUPS?.length)return;syncing=true;
 try{
  const p=profile(),progress=await getProgress(),id=String(p.canonicalStudentId||ids(p)[0]||'');if(!id)return;const localGroups=groupMetadata(),nowIso=new Date().toISOString(),c=course(p),ref=doc(db,'progress',id);let result=null;
  await runTransaction(db,async tx=>{
    const snap=await tx.get(ref),server=snap.exists()?(snap.data()||{}):{},serverGroups=server?.metadata?.verbenGroups||{},groups=mergeGroups(localGroups,serverGroups),beforeGroup=groupPoints(serverGroups),afterGroup=groupPoints(groups),metadata={...(server.metadata||{})};
    const hasBaseline=Object.prototype.hasOwnProperty.call(metadata,'verbenPointsBaselineV2'),savedBaseline=point(metadata.verbenPointsBaselineV2),baseline=Math.max(savedBaseline,beforeGroup),delta=hasBaseline?Math.max(0,afterGroup-baseline):0;
    const verben={...(server.verben||{})},actualTopics=Object.entries(verben).filter(([key])=>key!==CARRY_ID).reduce((sum,[,topic])=>sum+point(topic?.lifetime?.points),0),carryNeeded=Math.max(0,afterGroup-actualTopics),oldCarry=verben[CARRY_ID]||{};
    if(carryNeeded>0)verben[CARRY_ID]={...oldCarry,title:'Verben · wiederhergestellte Punkte',moduleTitle:'Verben',level:'A1',technicalRecovery:true,progressPercent:Number(oldCarry.progressPercent)||0,current:{...(oldCarry.current||{}),updatedAt:nowIso},lifetime:{...(oldCarry.lifetime||{}),points:carryNeeded}};
    else if(verben[CARRY_ID])verben[CARRY_ID]={...verben[CARRY_ID],lifetime:{...(verben[CARRY_ID].lifetime||{}),points:0}};
    metadata.verbenGroups=groups;metadata.verbenPointsBaselineV2=afterGroup;metadata.verbenPointsSyncV2={at:nowIso,beforeGroup,afterGroup,delta,baselineInitialized:!hasBaseline,autoLowering:false,transactional:true,noHistoricalAutoCredit:true};metadata.pointRecovery={...(metadata.pointRecovery||{}),verbenApplied:Math.max(point(metadata.pointRecovery?.verbenApplied),carryNeeded),verbenTarget:Math.max(point(metadata.pointRecovery?.verbenTarget),afterGroup),verbenUpdatedAt:nowIso,method:'group-baseline-transactional-v2'};
    const draft={...server,verben,metadata},structured=evidence(draft),floor=Math.max(storedPoints(server),storedPoints(progress)),rankingPoints=hasBaseline?floor+delta:floor,patch={verben,metadata,ranking:{...(server.ranking||{}),points:rankingPoints,updatedAt:nowIso},totals:{...(server.totals||{}),points:rankingPoints,updatedAt:nowIso},lifetimePoints:rankingPoints,pointsTotal:rankingPoints,punkteGesamt:rankingPoints,studentId:id,userId:id,docId:id,canonicalStudentId:id,aliasIds:uniq([...(server.aliasIds||[]),...ids(p)]),studentName:displayName(p),email:p.email||server.email||'',kurs:c||server.kurs||'',kursnummer:c||server.kursnummer||'',courseCode:c||server.courseCode||'',lastActive:serverTimestamp(),updatedAt:serverTimestamp(),lastActiveAt:nowIso,lastPage:location.pathname};
    tx.set(ref,patch,{merge:true});result={verbenPoints:afterGroup,delta,rankingPoints,structuredPoints:structured,baselineInitialized:!hasBaseline};
  });
  if(result){try{window.SPVerbRegroupRecovery?.preserveFloor?.(result.verbenPoints)}catch{}try{localStorage.setItem('SP_POINTS_TOTAL',String(Math.max(point(localStorage.getItem('SP_POINTS_TOTAL')),result.rankingPoints)));localStorage.setItem('SP_VERBEN_FIREBASE_POINTS_SYNC',nowIso)}catch{}window.SP_VERBEN_FIREBASE_SYNC={ok:true,...result,autoLowering:false,transactional:true,noHistoricalAutoCredit:true,at:nowIso}}
 }catch(error){console.warn('Verben-Punkte konnten nicht mit Firebase/Rangliste synchronisiert werden',error);window.SP_VERBEN_FIREBASE_SYNC={ok:false,error:String(error?.message||error),at:new Date().toISOString()}}
 finally{syncing=false}
}
function schedule(delay=700){clearTimeout(timer);timer=setTimeout(sync,delay)}
window.addEventListener('load',()=>schedule(800));window.addEventListener('pageshow',()=>schedule(500));window.addEventListener('pagehide',()=>schedule(0));document.addEventListener('click',()=>schedule(900));document.addEventListener('keydown',e=>{if(e.key==='Enter')schedule(900)});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='hidden')schedule(0)});new MutationObserver(()=>schedule(1000)).observe(document.documentElement,{childList:true,subtree:true});setTimeout(()=>schedule(0),1400);
window.SPVerbFirebaseRankingSync={sync,schedule};