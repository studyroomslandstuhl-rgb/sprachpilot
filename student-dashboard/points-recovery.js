import { db, doc, getDoc, setDoc, serverTimestamp, collection, query, where, getDocs, limit } from '/js/firebase.js';
import { getActiveProfile, getActiveRole } from '/js/auth.js';
import '/shared/points-recalculator.js?v=1';

const MODULES=['fragen','wortschatz','verben','perfekt','grammatik'];
const TECH=new Set(['state','progress','totals','metadata','profile','updatedAt','lastActive','lastPage','known','learned','unknown','unsure','activeVerbs','learnedVerbs']);
const num=v=>{const n=Number(v);return Number.isFinite(n)&&n>0?n:0};
const clamp=v=>Math.max(0,Math.min(100,Math.round(Number(v)||0)));
const uniq=a=>[...new Set((a||[]).filter(Boolean).map(String))];
const norm=s=>String(s||'').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
function profile(){return getActiveProfile()||{}}
function course(p=profile()){return String(p.courseCode||p.kurs||p.kursnummer||p.course||p.courseDocId||localStorage.getItem('SP_COURSE_CODE')||'').trim()}
function mail(p=profile()){return String(p.email||'').trim().toLowerCase()}
function fallbackId(p=profile()){const c=norm(p.courseDocId||course(p)||'kurs'),person=norm(mail(p)||p.vorname||p.firstName||p.name||'student');return c&&person?c+'_'+person:''}
function baseIds(p=profile()){const explicit=uniq([p.docId,p.studentId,p.userId,p.uid,p.id,fallbackId(p)]).filter(id=>id&&id!=='guest'),local=String(localStorage.getItem('SP_STUDENT_ID')||'').trim();if(local&&(!explicit.length||explicit.includes(local)))explicit.push(local);return uniq(explicit)}
function isStudent(){const r=String(getActiveRole()||localStorage.getItem('SP_LOGIN_ROLE')||'student').toLowerCase();return !['teacher','lehrer','admin'].includes(r)}
function isTopic(key,value){return !TECH.has(key)&&!!(value&&typeof value==='object'&&!Array.isArray(value)&&(value.lifetime||value.tasks||value.exam||value.current||value.progressPercent!=null||value.pointsTotal!=null))}
function maxMap(a={},b={}){const out={};for(const k of new Set([...Object.keys(a||{}),...Object.keys(b||{})]))out[k]=Math.max(num(a?.[k]),num(b?.[k]));return out}
function mergeTaskRuns(a={},b={}){const out={};for(const k of new Set([...Object.keys(a||{}),...Object.keys(b||{})]))out[k]=maxMap(a?.[k]||{},b?.[k]||{});return out}
function mergeTask(a={},b={}){return {...a,...b,percent:Math.max(clamp(a.percent),clamp(b.percent)),completed:!!(a.completed||b.completed),done:Math.max(Number(a.done||0),Number(b.done||0)),total:Math.max(Number(a.total||0),Number(b.total||0)),pointsByRun:maxMap(a.pointsByRun,b.pointsByRun)}}
function mergeTopic(a={},b={}){const out={...a,...b},tasks={...(a.tasks||{})};for(const[k,v]of Object.entries(b.tasks||{}))tasks[k]=mergeTask(tasks[k]||{},v||{});out.tasks=tasks;const al=a.lifetime||{},bl=b.lifetime||{};out.lifetime={...al,...bl,taskPointRuns:mergeTaskRuns(al.taskPointRuns||{},bl.taskPointRuns||{}),examPointRuns:maxMap(al.examPointRuns||{},bl.examPointRuns||{}),resets:Math.max(Number(al.resets||0),Number(bl.resets||0)),finishedRuns:Math.max(Number(al.finishedRuns||0),Number(bl.finishedRuns||0))};const ae=a.exam||{},be=b.exam||{};out.exam={...ae,...be,bestPercent:Math.max(Number(ae.bestPercent||ae.percent||0),Number(be.bestPercent||be.percent||0)),percent:Math.max(Number(ae.percent||0),Number(be.percent||0)),stars:Math.max(Number(ae.stars||0),Number(be.stars||0)),attempts:Math.max(Number(ae.attempts||0),Number(be.attempts||0)),completed:!!(ae.completed||be.completed),attempted:!!(ae.attempted||be.attempted)};const vals=Object.values(tasks);out.completedTasks=Math.max(Number(a.completedTasks||0),Number(b.completedTasks||0),vals.filter(t=>t&&t.completed).length);out.totalTasks=Math.max(Number(a.totalTasks||0),Number(b.totalTasks||0),vals.length);out.progressPercent=Math.max(clamp(a.progressPercent||a.current?.percent||0),clamp(b.progressPercent||b.current?.percent||0));out.current={...(a.current||{}),...(b.current||{}),percent:out.progressPercent,completedTasks:out.completedTasks,totalTasks:out.totalTasks};out.lifetime.points=Number(window.SPPointRecalculator?.topicPoints?.(out)?.points)||0;return out}
function topicSig(module,key,t={}){if(t.technicalRecovery)return module+'|tech|'+norm(key);if(t.topicId||t.themeId)return module+'|id|'+norm(t.topicId||t.themeId);const lesson=norm(t.lesson||t.lektion),theme=norm(t.theme||t.thema),level=norm(t.level);if(lesson||theme)return[module,level,lesson,theme].join('|');return module+'|key|'+norm(key)}
function mergeModule(base={},incoming={},module){const out={...(base||{})},bySig=new Map();for(const[k,v]of Object.entries(out)){if(isTopic(k,v))bySig.set(topicSig(module,k,v),k)}for(const[k,v]of Object.entries(incoming||{})){if(!isTopic(k,v)){if(!(k in out))out[k]=v;continue}const sig=topicSig(module,k,v),oldKey=bySig.get(sig);if(oldKey)out[oldKey]=mergeTopic(out[oldKey]||{},v||{});else{out[k]=mergeTopic({},v||{});bySig.set(sig,k)}}return out}
function mergeProgress(base={},incoming={}){const out={...base,...incoming};for(const m of MODULES)out[m]=mergeModule(base[m]||{},incoming[m]||{},m);out.metadata={...(base.metadata||{}),...(incoming.metadata||{})};if(base.finnischVerben||incoming.finnischVerben)out.finnischVerben={...(base.finnischVerben||{}),...(incoming.finnischVerben||{})};return out}
async function collectDocs(p){const queue=baseIds(p).slice(),seen=new Set(),rows=[];while(queue.length){const id=queue.shift();if(!id||seen.has(id))continue;seen.add(id);try{const s=await getDoc(doc(db,'progress',id));if(!s.exists())continue;const data=s.data()||{};rows.push({id,data});uniq(data.aliasIds||[]).forEach(a=>{if(!seen.has(a))queue.push(a)})}catch(e){}}const emails=uniq([mail(p),String(p.email||'').trim()]).filter(Boolean);for(const e of emails){try{const s=await getDocs(query(collection(db,'progress'),where('email','==',e),limit(20)));for(const d of s.docs){if(seen.has(d.id))continue;seen.add(d.id);const data=d.data()||{};rows.push({id:d.id,data});uniq(data.aliasIds||[]).forEach(a=>{if(!seen.has(a))queue.push(a)})}}catch(e){}}return rows}
export async function repairDashboardPoints(){
 if(!isStudent())return{ok:false,reason:'preview'};const p=profile(),ids=baseIds(p),canonical=ids[0]||fallbackId(p);if(!canonical)return{ok:false,reason:'no-id'};const rows=await collectDocs(p);let merged={};const aliases=new Set(ids);for(const row of rows){aliases.add(row.id);uniq(row.data.aliasIds||[]).forEach(x=>aliases.add(x));merged=mergeProgress(merged,row.data)}
 const audit=window.SPPointRecalculator?.audit?.(merged)||{total:0,stored:0,difference:0,inflatedBy:0,breakdown:{}};const corrected=Number(audit.total)||0,nowIso=new Date().toISOString(),patch={};for(const m of MODULES)if(merged[m]&&Object.keys(merged[m]).length)patch[m]=merged[m];if(merged.finnischVerben)patch.finnischVerben=merged.finnischVerben;patch.ranking={...(merged.ranking||{}),points:corrected,updatedAt:nowIso};patch.totals={...(merged.totals||{}),points:corrected,updatedAt:nowIso};patch.pointsTotal=corrected;patch.lifetimePoints=corrected;patch.punkteGesamt=corrected;patch.studentId=canonical;patch.userId=canonical;patch.docId=canonical;patch.canonicalStudentId=canonical;patch.aliasIds=[...aliases];patch.email=p.email||merged.email||'';patch.kurs=course(p)||merged.kurs||'';patch.kursnummer=course(p)||merged.kursnummer||'';patch.courseCode=course(p)||merged.courseCode||'';patch.updatedAt=serverTimestamp();patch.lastActive=serverTimestamp();patch.metadata={...(merged.metadata||{}),pointAudit:{...(merged.metadata?.pointAudit||{}),version:4,lastRepairAt:nowIso,sourceIds:rows.map(r=>r.id),storedBefore:audit.stored,correctedPoints:corrected,inflatedBy:audit.inflatedBy,breakdown:audit.breakdown,method:'verified-progress-evidence'}};
 try{await setDoc(doc(db,'progress',canonical),patch,{merge:true});const aliasPatch={ranking:{points:corrected,updatedAt:nowIso},totals:{points:corrected,updatedAt:nowIso},pointsTotal:corrected,lifetimePoints:corrected,punkteGesamt:corrected,metadata:{pointAudit:{version:4,lastRepairAt:nowIso,correctedPoints:corrected,inflatedBy:audit.inflatedBy,method:'verified-progress-evidence'}}};await Promise.all(rows.filter(r=>r.id!==canonical).map(r=>setDoc(doc(db,'progress',r.id),aliasPatch,{merge:true}).catch(()=>null)));localStorage.setItem('SP_STUDENT_ID',canonical);localStorage.setItem('SP_POINTS_OWNER_ID',canonical);localStorage.setItem('SP_POINTS_TOTAL',String(corrected));localStorage.setItem('SP_POINTS_AUDIT_LAST',JSON.stringify({at:nowIso,corrected,storedBefore:audit.stored,inflatedBy:audit.inflatedBy,breakdown:audit.breakdown}));window.SP_POINTS_AUDIT={ok:true,corrected,storedBefore:audit.stored,inflatedBy:audit.inflatedBy,sources:rows.map(r=>r.id),breakdown:audit.breakdown};return window.SP_POINTS_AUDIT}catch(error){window.SP_POINTS_AUDIT={ok:false,error:String(error?.message||error),corrected,storedBefore:audit.stored,inflatedBy:audit.inflatedBy,breakdown:audit.breakdown};return window.SP_POINTS_AUDIT}
}
