(function(){
'use strict';
if(window.__SP_L7T4_UI_POLISH_V1)return;window.__SP_L7T4_UI_POLISH_V1=true;
function install(){
 if(!window.L7||!window.L7S||window.L7.__l7t4UiPolishV1)return false;
 const S=window.L7S,raw=window.L7.renderTaskPage.bind(window.L7),theme=4;
 const HANDLED=new Set(['phone-gap-two-stage','email-input-cloze','message-reading','school-email-reading','dialog-bubbles','error-correct']);
 const esc=v=>S.esc(v),norm=v=>S.norm(v);
 function style(){if(document.getElementById('l7t4-ui-polish-style'))return;const x=document.createElement('style');x.id='l7t4-ui-polish-style';x.textContent=`
 .l7-question-card,.l7-learning{text-align:left!important}.sp-t4-progress{margin-bottom:16px}.sp-t4-dialog-frame{max-width:720px;margin:18px auto 24px;padding:18px;border:7px solid #292633;border-radius:30px;background:#f3f4f7;box-shadow:0 10px 28px rgba(0,0,0,.12)}.sp-t4-dialog-list{display:grid;gap:12px}.sp-t4-bubble{max-width:84%;padding:12px 15px;border-radius:18px;background:#fff;border:1px solid #ddd7f6;line-height:1.55;font-size:17px}.sp-t4-bubble.right{justify-self:end;background:#e5f4ff;border-color:#cae8f7}.sp-t4-speaker{font-size:12px;font-weight:950;color:#665e77;margin-bottom:4px}.sp-t4-gap-field select,.sp-t4-gap-field input{max-width:220px;padding:7px 9px;border:2px solid #bfb4e8;border-radius:10px;background:#fff;font:inherit}.sp-t4-stage{font-weight:950;font-size:18px;margin:4px 0 12px}.sp-t4-email{max-width:800px;margin:16px auto 24px;border:1px solid #d6d9e1;border-radius:16px;overflow:hidden;background:#fff;box-shadow:0 6px 20px rgba(0,0,0,.08);text-align:left}.sp-t4-email-meta{display:grid;gap:5px;padding:13px 16px;background:#f3f4f7;border-bottom:1px solid #d6d9e1;font-size:14px}.sp-t4-email-body{padding:20px;white-space:pre-wrap;line-height:1.75;font-size:18px;text-align:left}.sp-t4-email-body select,.sp-t4-email-body input{display:inline-block;max-width:190px;padding:6px 8px;border:0;border-bottom:2px solid #6d59b4;background:#faf9ff;font:inherit;font-weight:850}.sp-t4-message{max-width:760px;margin:14px auto 22px;padding:20px;border:2px solid #ddd7f6;border-radius:22px;background:#fff;box-shadow:0 5px 18px rgba(0,0,0,.07);font-size:18px;line-height:1.75;text-align:left;white-space:pre-wrap}.sp-t4-message-head{font-size:13px;font-weight:950;color:#665e77;margin-bottom:10px}.sp-t4-q{padding:18px 0;border-top:1px solid #ebe7f8}.sp-t4-q:first-of-type{border-top:0}.sp-t4-q>strong{font-size:18px}.sp-t4-options{display:grid;gap:9px;margin-top:10px}.sp-t4-options label,.sp-t4-options button{display:flex;gap:10px;align-items:flex-start;padding:12px 14px;border:1px solid #d7d1ed;border-radius:13px;background:#fff;text-align:left;font:inherit;cursor:pointer}.sp-t4-options label:has(input:checked){background:#f6f3ff;border-color:#8874d4}.sp-t4-what{font-size:22px;font-weight:950;margin:28px 0 10px}.sp-t4-error-sentence{font-size:clamp(29px,4.1vw,39px);line-height:1.7;margin:22px 0}.sp-t4-error-sentence button{display:inline;border:0;background:transparent;padding:2px 4px;border-radius:7px;font:inherit;font-weight:760;cursor:pointer}.sp-t4-error-sentence button:hover,.sp-t4-error-sentence button:focus{background:var(--soft);outline:2px solid var(--line)}.sp-t4-error-sentence button.selected{background:#fff0a8;outline:3px solid #dfb72c}.sp-t4-answer{display:grid;gap:8px;max-width:680px;margin:16px 0}.sp-t4-answer input{width:100%;padding:12px;border:2px solid #d9d4f4;border-radius:12px;font:inherit}.sp-t4-help{margin-top:12px}.sp-t4-actions{margin-top:16px}.sp-t4-actions .l7-btn{min-width:180px}.sp-t4-gap-num{font-size:12px;color:#6d647a;font-weight:900;margin-left:4px}@media(max-width:650px){.sp-t4-dialog-frame{border-width:5px;padding:12px}.sp-t4-bubble{max-width:94%;font-size:16px}.sp-t4-email-body,.sp-t4-message{font-size:17px}.sp-t4-email-body select,.sp-t4-email-body input{max-width:145px}}
 `;document.head.appendChild(x)}
 function doneCount(st,total){return new Set((st.done||[]).map(Number).filter(i=>Number.isInteger(i)&&i>=0&&i<total)).size}
 function progress(t){const st=S.load(theme,t.id,t.items.length),d=doneCount(st,t.items.length),p=S.pct(theme,t.id,t.items.length);return`<div class="sp-t4-progress"><div class="l7-progress-row"><span>${d} fehlerfrei · ${t.items.length-d} übrig</span><strong>${p}%</strong></div><div class="l7-progress"><span style="width:${p}%"></span></div></div>`}
 function shell(t,index,body){const ins=Object.prototype.hasOwnProperty.call(t,'instruction')?String(t.instruction||''):String(t.description||'');document.getElementById('app').innerHTML=`<div class="l7-page">${S.header(theme,t.title)}<section class="l7-card">${progress(t)}${ins?`<div class="l7-instruction">${esc(ins)}</div>`:''}<div class="l7-question-card"><p class="eyebrow">${index+1} von ${t.items.length}</p>${body}<div id="feedback" class="sp-t4-help"></div></div></section><footer>© SprachPilot</footer></div>`;setTimeout(()=>document.querySelector('.l7-question-card')?.scrollIntoView({block:'start',behavior:'smooth'}),50)}
 function key(index,name){return`polish:${index}:${name}`}
 function state(t){return S.load(theme,t.id,t.items.length)}
 function get(t,index,name,def=''){return state(t).answers?.[key(index,name)]??def}
 function set(t,index,name,value){const st=state(t);st.answers=st.answers||{};if(value==null)delete st.answers[key(index,name)];else st.answers[key(index,name)]=value;S.save(theme,t.id,st,false)}
 function clear(t,index,names){const st=state(t);st.answers=st.answers||{};names.forEach(name=>delete st.answers[key(index,name)]);S.save(theme,t.id,st,false)}
 function lock(){document.querySelectorAll('#app button,#app input,#app select,#app textarea').forEach(el=>el.disabled=true)}
 function fail(t,index,hint,solution){S.attempt(theme,t.id,t.items.length,index,false);const count=Number(S.wrong(theme,t.id,t.items.length))||1;let html='';if(count===1)html='<div class="l7-no">Noch nicht richtig. Diese Aufgabe kommt später noch einmal.</div>';else if(count===2)html=`<div class="l7-hint"><strong>Hinweis:</strong> ${esc(hint||'Lies oder höre noch einmal genau.')}</div>`;else html=`<div class="l7-hint"><strong>Lösung:</strong> ${esc(solution||'Prüfe die richtige Lösung.')}<br>Bearbeite die Aufgabe beim nächsten Durchgang wieder selbst.</div>`;const fb=document.getElementById('feedback');if(fb)fb.innerHTML=html;lock()}
 function pass(t,index,keys=[]){clear(t,index,keys);S.attempt(theme,t.id,t.items.length,index,true);S.right(theme,t.id,t.items.length,true);const fb=document.getElementById('feedback');if(fb)fb.innerHTML='<div class="l7-ok">Richtig.</div>';lock();setTimeout(()=>render(theme,t.id),520)}
 function fieldHtml(blank,n,phase,value){if(phase==='select')return`<span class="sp-t4-gap-field"><select data-gap="${n}"><option value="">— wählen —</option>${(blank.options||[]).map(o=>`<option value="${esc(o)}" ${value===o?'selected':''}>${esc(o)}</option>`).join('')}</select><span class="sp-t4-gap-num">${n+1}</span></span>`;return`<span class="sp-t4-gap-field"><input data-gap="${n}" value="${esc(value||'')}" autocomplete="off"><span class="sp-t4-gap-num">${n+1}</span></span>`}
 function lineWithGaps(text,blanks,phase,values){let out=esc(text);(blanks||[]).forEach((b,n)=>{out=out.replace(`{{${n}}}`,fieldHtml(b,n,phase,values[n]||''))});return out}
 function phoneGap(t,index,item){const phase=String(get(t,index,'phase','select')),values=Array.isArray(get(t,index,phase,[]))?get(t,index,phase,[]):[];const lines=String(item.text||'').split('\n').filter(Boolean);const bubbles=lines.map((line,li)=>{const cut=line.indexOf(':'),speaker=cut>=0?line.slice(0,cut):'',body=cut>=0?line.slice(cut+1).trim():line;return`<div class="sp-t4-bubble ${li%2?'right':''}">${speaker?`<div class="sp-t4-speaker">${esc(speaker)}</div>`:''}${lineWithGaps(body,item.blanks,phase,values)}</div>`}).join('');shell(t,index,`<div class="sp-t4-stage">${phase==='select'?'1. Wähle die passenden Wörter.':'2. Schreibe die Wörter jetzt selbst.'}</div><div class="sp-t4-dialog-frame"><div class="sp-t4-dialog-list">${bubbles}</div></div><div class="sp-t4-actions"><button class="l7-btn" id="gapCheck">${phase==='select'?'Auswahl prüfen':'Wörter prüfen'}</button></div>`);document.querySelectorAll('[data-gap]').forEach(el=>el.addEventListener(phase==='select'?'change':'input',()=>set(t,index,phase,[...document.querySelectorAll('[data-gap]')].map(x=>x.value))));document.getElementById('gapCheck').onclick=()=>{const vals=[...document.querySelectorAll('[data-gap]')].map(x=>String(x.value||'').trim());if(vals.some(v=>!v)){document.getElementById('feedback').innerHTML='<div class="l7-hint">Fülle zuerst alle Lücken aus.</div>';return}const wrong=[];(item.blanks||[]).forEach((b,n)=>{if(norm(vals[n])!==norm(b.answer))wrong.push(n)});if(wrong.length){set(t,index,phase,[]);const sol=wrong.map(n=>`${n+1}: ${item.blanks[n].answer}`).join(' · ');return fail(t,index,`Prüfe ${wrong.length===1?'Lücke':'die Lücken'} ${wrong.map(n=>n+1).join(', ')}.`,sol)}if(phase==='select'){set(t,index,'select',[]);set(t,index,'write',[]);set(t,index,'phase','write');return render(theme,t.id)}pass(t,index,['phase','select','write'])}}
 function emailCloze(t,index,item){const phase=String(get(t,index,'phase','select')),values=Array.isArray(get(t,index,phase,[]))?get(t,index,phase,[]):[];const body=lineWithGaps(item.text,item.blanks,phase,values);shell(t,index,`<div class="sp-t4-stage">${phase==='select'?'1. Wähle die passenden Wörter.':'2. Schreibe dieselbe E-Mail jetzt selbst.'}</div><div class="sp-t4-email"><div class="sp-t4-email-meta"><div><b>Von:</b> ${esc(item.from||'')}</div><div><b>Betreff:</b> ${esc(item.subject||'')}</div></div><div class="sp-t4-email-body">${body}</div></div><div class="sp-t4-actions"><button class="l7-btn" id="emailCheck">${phase==='select'?'Auswahl prüfen':'E-Mail prüfen'}</button></div>`);document.querySelectorAll('[data-gap]').forEach(el=>el.addEventListener(phase==='select'?'change':'input',()=>set(t,index,phase,[...document.querySelectorAll('[data-gap]')].map(x=>x.value))));document.getElementById('emailCheck').onclick=()=>{const vals=[...document.querySelectorAll('[data-gap]')].map(x=>String(x.value||'').trim());if(vals.some(v=>!v)){document.getElementById('feedback').innerHTML='<div class="l7-hint">Fülle zuerst alle Lücken aus.</div>';return}const wrong=[];(item.blanks||[]).forEach((b,n)=>{if(norm(vals[n])!==norm(b.answer))wrong.push(n)});if(wrong.length){set(t,index,phase,[]);const sol=wrong.map(n=>`${n+1}: ${item.blanks[n].answer}`).join(' · ');return fail(t,index,`Achte auf Anrede, Verbform und passenden Abschluss. Prüfe Lücke ${wrong.map(n=>n+1).join(', ')}.`,sol)}if(phase==='select'){set(t,index,'select',[]);set(t,index,'write',[]);set(t,index,'phase','write');return render(theme,t.id)}pass(t,index,['phase','select','write'])}}
 function card(item,mode){if(mode==='email'||item.format==='email')return`<div class="sp-t4-email"><div class="sp-t4-email-meta"><div><b>Von:</b> ${esc(item.from||'Schule')}</div>${item.subject?`<div><b>Betreff:</b> ${esc(item.subject)}</div>`:''}</div><div class="sp-t4-email-body">${esc(item.text||'')}</div></div>`;return`<div class="sp-t4-message"><div class="sp-t4-message-head">${esc(item.from||'Nachricht')}</div>${esc(item.text||'')}</div>`}
 function reading(t,index,item,mode){const saved=get(t,index,'questions',{})||{};shell(t,index,`${card(item,mode)}${(item.questions||[]).map((qq,qi)=>`<div class="sp-t4-q"><strong>${qi+1}. ${esc(qq.q)}</strong><div class="sp-t4-options">${(qq.options||[]).map((o,n)=>`<label><input type="radio" name="rq${qi}" value="${esc(o)}" ${saved[qi]===o?'checked':''}><span><b>${String.fromCharCode(65+n)}.</b> ${esc(o)}</span></label>`).join('')}</div></div>`).join('')}<div class="sp-t4-actions"><button class="l7-btn" id="readingCheck">Antworten prüfen</button></div>`);document.querySelectorAll('.sp-t4-options input').forEach(el=>el.onchange=()=>{const out={};(item.questions||[]).forEach((qq,qi)=>out[qi]=document.querySelector(`input[name="rq${qi}"]:checked`)?.value||'');set(t,index,'questions',out)});document.getElementById('readingCheck').onclick=()=>{const missing=[],wrong=[];(item.questions||[]).forEach((qq,qi)=>{const v=document.querySelector(`input[name="rq${qi}"]:checked`)?.value||'';if(!v)missing.push(qi);else if(norm(v)!==norm(qq.answer))wrong.push(qi)});if(missing.length){document.getElementById('feedback').innerHTML='<div class="l7-hint">Beantworte zuerst alle Fragen.</div>';return}if(wrong.length){set(t,index,'questions',{});const sol=wrong.map(n=>`${n+1}: ${item.questions[n].answer}`).join(' · ');return fail(t,index,`Lies die Nachricht noch einmal und prüfe Frage ${wrong.map(n=>n+1).join(', ')}.`,sol)}pass(t,index,['questions'])}}
 function dialogue(t,index,item){shell(t,index,`<div class="sp-t4-dialog-frame"><div class="sp-t4-dialog-list">${(item.turns||[]).map((turn,n)=>`<div class="sp-t4-bubble ${n%2?'right':''}"><div class="sp-t4-speaker">${esc(turn[0])}</div>${String(turn[1]).includes('{{gap}}')?'…':esc(turn[1])}</div>`).join('')}</div></div><div class="sp-t4-what">Was passt?</div><div class="sp-t4-options">${(item.options||[]).map((o,n)=>`<button type="button" data-dialog="${esc(o)}"><b>${String.fromCharCode(65+n)}.</b>&nbsp; ${esc(o)}</button>`).join('')}</div>`);document.querySelectorAll('[data-dialog]').forEach(b=>b.onclick=()=>{if(norm(b.dataset.dialog)===norm(item.answer))return pass(t,index,[]);fail(t,index,'Achte auf Person, Grammatik und den Zusammenhang im ganzen Dialog.',item.answer)})}
 function errorCorrect(t,index,item){clear(t,index,['errSel']);let selected=-1;const draft=String(get(t,index,'errInput','')||''),words=String(item.sentence||'').split(/\s+/);shell(t,index,`<div class="sp-t4-error-sentence">${words.map((w,n)=>`<button type="button" data-word="${n}">${esc(w)}</button>`).join(' ')}</div><div class="sp-t4-answer"><label for="errInput"><strong>Richtige Form</strong></label><input id="errInput" value="${esc(draft)}" autocomplete="off"><button class="l7-btn" id="errCheck">Prüfen</button></div>`);document.querySelectorAll('[data-word]').forEach(b=>b.onclick=()=>{selected=Number(b.dataset.word);document.querySelectorAll('[data-word]').forEach(x=>x.classList.toggle('selected',x===b))});const input=document.getElementById('errInput');input.oninput=()=>set(t,index,'errInput',input.value);const check=()=>{const value=String(input.value||'').trim();if(selected<0||!value){document.getElementById('feedback').innerHTML='<div class="l7-hint">Klicke zuerst das falsche Wort an und schreibe danach die richtige Form.</div>';return}const chosen=norm(words[selected]),wrongWord=norm(item.wrongWord),formOk=norm(value)===norm(item.answer);if(chosen===wrongWord&&formOk)return pass(t,index,['errSel','errInput']);set(t,index,'errInput','');fail(t,index,chosen!==wrongWord?'Suche zuerst das Wort mit dem Rechtschreibfehler.':'Achte auf die richtige Schreibweise.',`${item.wrongWord} → ${item.answer}`)};document.getElementById('errCheck').onclick=check;input.onkeydown=e=>{if(e.key==='Enter')check()}}
 function render(th,id){const t=S.task(id);if(!t||!HANDLED.has(t.kind))return raw(th,id);const st=state(t);if(doneCount(st,t.items.length)>=t.items.length)return raw(th,id);const index=S.index(theme,t.id,t.items.length),item=t.items[index];if(index==null||!item)return raw(th,id);if(t.kind==='phone-gap-two-stage')return phoneGap(t,index,item);if(t.kind==='email-input-cloze')return emailCloze(t,index,item);if(t.kind==='message-reading')return reading(t,index,item,'message');if(t.kind==='school-email-reading')return reading(t,index,item,'email');if(t.kind==='dialog-bubbles')return dialogue(t,index,item);if(t.kind==='error-correct')return errorCorrect(t,index,item);return raw(th,id)}
 style();window.L7.renderTaskPage=render;window.L7.__l7t4UiPolishV1=true;return true
}
window.L7T4UIPolish={install};if(!install()){let tries=0;const timer=setInterval(()=>{if(install()||++tries>200)clearInterval(timer)},25)}
})();
