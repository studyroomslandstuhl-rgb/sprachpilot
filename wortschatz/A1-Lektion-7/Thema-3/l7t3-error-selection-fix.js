(function(){
'use strict';
if(window.__SP_L7T3_ERROR_SELECTION_FIX_V2)return;window.__SP_L7T3_ERROR_SELECTION_FIX_V2=true;
function install(){
 if(!window.L7||!window.L7S||window.L7.__spL7T3ErrorSelectionFixV2)return false;
 const S=window.L7S,raw=window.L7.renderTaskPage.bind(window.L7),theme=3,id='t3-fehler-korrigieren-v2',esc=S.esc;
 const clean=v=>S.norm(String(v||'').replace(/[.,!?;:“”„"'`()]/g,''));
 function render(th,taskId){
  if(String(taskId)!==id)return raw(th,taskId);
  const t=S.task(id);if(!t)return raw(th,taskId);const st=S.load(theme,t.id,t.items.length);if((st.done||[]).length>=t.items.length)return raw(th,taskId);const index=S.index(theme,t.id,t.items.length),item=t.items[index];if(index==null||!item)return raw(th,taskId);
  const fresh=S.load(theme,t.id,t.items.length);fresh.answers=fresh.answers||{};let selected=Number(fresh.answers[`errSel:${index}`]);if(!Number.isInteger(selected))selected=-1;const draft=String(fresh.answers[`errInput:${index}`]||''),words=String(item.sentence||'').split(/\s+/),pct=S.pct(theme,t.id,t.items.length),tries=Number(fresh.tries||0);
  document.getElementById('app').innerHTML=`<div class="l7-page">${S.header(theme,t.title)}<section class="l7-card"><div class="l7-progress-row"><span>${fresh.done.length} fehlerfrei · ${t.items.length-fresh.done.length} übrig</span><strong>${pct}%</strong></div><div class="l7-progress"><span style="width:${pct}%"></span></div><div class="l7-instruction">${esc(t.description||'1. Klicke das falsche Wort an. 2. Schreibe die richtige Form.')}</div><div class="l7-question-card"><div class="sp-error-sentence">${words.map((word,n)=>`<button type="button" data-error-word="${n}" class="${n===selected?'selected':''}">${esc(word)}</button>`).join(' ')}</div><div class="l7-answer-box"><label for="spErrorInput">Richtige Form</label><div><input id="spErrorInput" value="${esc(draft)}" autocomplete="off"><button class="l7-btn" id="spErrorCheck">Prüfen</button></div></div><div id="spFeedback">${feedbackFor(tries,item)}</div></div></section></div>`;
  setTimeout(()=>document.querySelector('.l7-question-card')?.scrollIntoView({block:'start',behavior:'smooth'}),40);
  document.querySelectorAll('[data-error-word]').forEach(btn=>btn.onclick=()=>{selected=Number(btn.dataset.errorWord);const x=S.load(theme,t.id,t.items.length);x.answers=x.answers||{};x.answers[`errSel:${index}`]=selected;S.save(theme,t.id,x,false);document.querySelectorAll('[data-error-word]').forEach(node=>node.classList.toggle('selected',node===btn))});
  const input=document.getElementById('spErrorInput');input.oninput=()=>{const x=S.load(theme,t.id,t.items.length);x.answers=x.answers||{};x.answers[`errInput:${index}`]=input.value;S.save(theme,t.id,x,false)};
  const check=()=>{const value=String(input.value||'').trim();if(selected<0||!value){document.getElementById('spFeedback').innerHTML='<div class="l7-hint">Klicke zuerst selbst das falsche Wort an und schreibe danach die richtige Form.</div>';return}const okWord=clean(words[selected])===clean(item.wrongWord),accepted=[item.answer,...(item.answers||[])].filter(Boolean),okForm=accepted.some(answer=>S.norm(value)===S.norm(answer));if(okWord&&okForm){const x=S.load(theme,t.id,t.items.length);delete x.answers[`errInput:${index}`];delete x.answers[`errSel:${index}`];S.save(theme,t.id,x,false);S.attempt(theme,t.id,t.items.length,index,true);S.right(theme,t.id,t.items.length,true);document.querySelectorAll('#app button,#app input').forEach(node=>node.disabled=true);document.getElementById('spFeedback').innerHTML='<div class="l7-ok">Richtig.</div>';setTimeout(()=>render(theme,id),520);return}S.attempt(theme,t.id,t.items.length,index,false);const count=Number(S.wrong(theme,t.id,t.items.length))||1;document.getElementById('spFeedback').innerHTML=feedbackFor(count,item,okWord)};
  document.getElementById('spErrorCheck').onclick=check;input.onkeydown=e=>{if(e.key==='Enter')check()};
 }
 function feedbackFor(count,item,wordCorrect=false){if(count===1)return'<div class="l7-no"><strong>Noch nicht richtig.</strong><br>Korrigiere deine Antwort und versuche es noch einmal.</div>';if(count===2)return`<div class="l7-hint"><strong>Hinweis:</strong> ${esc(wordCorrect?'Das falsche Wort ist richtig markiert. Prüfe jetzt die richtige Form.':`Suche den Fehler bei ${item.errorType||'diesem Wort'}.`)}</div>`;if(count>=3)return`<div class="l7-hint"><strong>Lösung:</strong> ${esc(item.wrongWord)} → ${esc(item.answer)}<br>Markiere das Wort und schreibe die Lösung selbst. Erst die richtige Antwort schließt die Aufgabe ab.</div>`;return''}
 window.L7.renderTaskPage=render;window.L7.__spL7T3ErrorSelectionFixV2=true;return true
}
window.L7T3ErrorSelectionFix={install};
})();