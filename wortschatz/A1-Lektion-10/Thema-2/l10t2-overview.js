import {renderSpHeader,bindSpHeader} from '/js/sp-header.js?v=theme-standard4';
const D=window.L10T2||{tasks:[]};const root=document.getElementById('app');const TOPIC='wortschatz-a1-lektion-10-thema-2';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function preview(){try{return ['teacher','lehrer','admin','owner','superadmin'].includes(String(localStorage.getItem('SP_LOGIN_ROLE')||localStorage.getItem('SP_ACTIVE_ROLE')||'').toLowerCase())||sessionStorage.getItem('SP_TEACHER_PREVIEW')==='1'||localStorage.getItem('SP_TEACHER_PREVIEW')==='1'}catch(e){return false}}
function run(){return Math.max(1,Math.min(3,Number(localStorage.getItem('SP_SCORE_RUN_'+TOPIC)||1)||1))}
function owner(){try{const p=JSON.parse(localStorage.getItem('SP_USER_PROFILE')||localStorage.getItem('SP_STUDENT_PROFILE')||'{}')||{};return String(p.canonicalStudentId||p.docId||p.studentId||p.userId||p.authUid||p.uid||p.id||p.email||localStorage.getItem('SP_STUDENT_ID')||'student').trim().toLowerCase().replace(/[^a-z0-9äöüß@._-]+/gi,'_')}catch(e){return'student'}}
function pct(t){if(preview())return 0;try{const s=JSON.parse(localStorage.getItem(`SP_L10_${owner()}_T2_${t.id}`)||'null');if(!s||Number(s._run||1)!==run())return 0;const total=Math.max(0,Number(s.total||0));const done=Array.isArray(s.done)?s.done.length:Math.max(0,Number(s.done||0));return total?Math.min(100,Math.round(done/total*100)):0}catch(e){return 0}}
const progress=Object.fromEntries((D.tasks||[]).map(t=>[t.id,pct(t)]));
if(!preview())try{const mod=await import('/js/progress.js?v=20260831-central6');const cloud=await mod.loadCurrentStudentProgress();const topic=cloud?.wortschatz?.[TOPIC];if(topic){for(const t of D.tasks||[]){const rec=topic.tasks?.[t.id]||topic.tasks?.[`task.html?task=${t.id}`];if(rec&&(Number(rec.run||topic.currentRun||run())===run()))progress[t.id]=Math.max(progress[t.id]||0,Math.max(0,Math.min(100,Number(rec.percent||0))))}}}catch(e){console.warn('L10T2 overview progress',e)}
const completed=(D.tasks||[]).filter(t=>(progress[t.id]||0)>=100).length;const avg=Math.round((D.tasks||[]).reduce((s,t)=>s+(progress[t.id]||0),0)/Math.max(1,(D.tasks||[]).length));const header=renderSpHeader({subtitle:'Gesundheit und Alltag · A1 Lektion 10 · Thema 2',color:{main:'#F4A3A3',dark:'#A86464',soft:'#FDF1F1',line:'#F3C9C9'}});
root.innerHTML=`<div class="l10-theme-page">${header}<div class="l8-wrap">${preview()?'<div class="sp-teacher-preview-note">Lehrer-Vorschau: Fortschritte und Punkte der Teilnehmer werden nicht verändert.</div>':''}<section class="l8-card l8-progress-card"><div class="l8-progress-circle">${avg}%</div><div class="l8-progress-main"><h2>Dein Fortschritt</h2><p class="l8-small">${completed} / ${(D.tasks||[]).length} Aufgaben abgeschlossen</p><div class="l8-progress"><div style="width:${avg}%"></div></div><p class="l8-small l8-theme-subtitle">Gesundheit, Schmerzen und Possessivpronomen</p><div class="l8-tags"><span class="l8-tag">21 Wörter</span><span class="l8-tag">wehtun</span><span class="l8-tag">-schmerzen</span><span class="l8-tag">Possessivpronomen</span></div></div><div class="l8-score-slot"><div class="l8-score-panel"><div class="l8-score-label">${preview()?'Punkte':run()===1?'Versuch 1 von 3':`Wiederholung ${run()} von 3`}</div><div class="l8-score-total">${preview()?'Vorschau':(Number(localStorage.getItem('SP_POINTS_TOTAL')||0)||0)+' Punkte'}</div><div class="l8-small">${preview()?'Keine Teilnehmerpunkte':'Fortschritt wird automatisch gespeichert.'}</div></div></div></section><section class="l8-grid">${(D.tasks||[]).map((t,i)=>{const p=Math.round(progress[t.id]||0);return `<a class="l8-card l8-task-card ${p>=100?'done':''}" href="task.html?task=${encodeURIComponent(t.id)}"><div class="l8-task-number">${i+1}. ${esc(t.title)}</div><div class="emoji">${esc(t.icon||'✅')}</div><p>${esc(t.text||'')}</p><div class="l8-progress"><div style="width:${p}%"></div></div><div class="l8-small">${p}%</div><div class="l8-task-start">${p>=100?'Fertig':'Starten'}</div></a>`}).join('')}</section><footer>© SprachPilot</footer></div></div>`;bindSpHeader(root);
