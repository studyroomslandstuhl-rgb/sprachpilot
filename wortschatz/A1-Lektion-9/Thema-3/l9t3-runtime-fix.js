(function(){'use strict';if(String(new URLSearchParams(location.search).get('task')||'')!=='duerfen-tabelle')return;document.addEventListener('click',e=>{if(e.target?.id!=='checkAll')return;setTimeout(()=>{if(!document.getElementById('checkAll'))return;const stores=[localStorage,sessionStorage];for(const st of stores){for(let i=0;i<st.length;i++){const k=st.key(i);if(!k||!k.endsWith('_T3_duerfen-tabelle'))continue;try{const s=JSON.parse(st.getItem(k)||'{}');const done=(s.done||[]).length,review=Object.values(s.review||{}).some(Boolean);if(done>=7&&review){location.reload();return;}}catch(err){}}}},520)},true);})();