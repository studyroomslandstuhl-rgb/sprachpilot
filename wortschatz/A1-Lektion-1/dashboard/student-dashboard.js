function renderStudentDashboard(){const rows=(state.active||[]).map(v=>`<div class="progress-row"><strong>${safeText(v)}</strong><br><span class="small">${safeText(nativeWord(v))}</span><div class="progress-line"><div class="progress-fill" style="width:${verbPercent(v)}%"></div></div><span class="small">${verbPercent(v)}%</span></div>`).join("");$("app").innerHTML=`<h2>Meine Statistik</h2><div class="stats"><div class="stat"><div>Fortschritt</div><div class="num">${overall()}%</div></div><div class="stat"><div>Sterne</div><div class="num">${totalStars()}</div></div><div class="stat"><div>Unsicher</div><div class="num">${state.unsure.length}</div></div><div class="stat"><div>Kann ich nicht</div><div class="num">${state.unknown.length}</div></div></div><h3>Aktive Verben</h3>${rows||"<p>Noch keine aktiven Verben.</p>"}<button class="secondary" onclick="renderHome()">Zurück</button>`}
