function verbToImage(){const v=nextFromTaskQueue("verb_bild");if(!v){renderHome();return}const options=shuffle([v,...shuffle(ALL_VERBS.map(x=>x.v).filter(x=>x!==v)).slice(0,3)]);$("app").innerHTML=`<h2>Verb → Bild</h2>${taskProgressHtml("verb_bild","Verb → Bild")}<div class="german-word">${safeText(v)}</div><div class="quiz-options">${options.map(o=>`<button class="quiz-option bild-option" onclick="checkVerbToImage('${safeText(v)}','${safeText(o)}')">${imageBox(o,true)}</button>`).join("")}</div><div id="fb"></div>`;renderAndHydrate()}
function checkVerbToImage(v,o){const good=v===o;$("fb").innerHTML=good?"<div class='ok'>Richtig.</div>":`<div class='no'>Falsch. Richtig ist: <strong>${safeText(v)}</strong></div>`;addEncounter(v,"verb_bild",good);finishQueuedVerb("verb_bild",v,good);setTimeout(verbToImage,700)}
